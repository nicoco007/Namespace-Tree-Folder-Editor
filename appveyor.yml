version: '{branch}-{build}'

configuration: Release

before_build:
  - nuget restore

build:
  verbosity: minimal

artifacts:
- path: 'Namespace Tree Folder Editor\bin\Release\Namespace Tree Folder Editor.exe'